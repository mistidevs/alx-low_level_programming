#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

int main(void)
{
int len1, len2, num1, num2, result;

num1 = 234758884
num2 = 877464630

while (num != 0)
{
num1 /= 10;
len1++;
}

while (num != 0)
{
num2 /= 10;
len2++;
}

while (i < len2)
{
	dig_str = av[2][len2 - i - 1];
	dig_num = atoi(&dig_str);
	result += (*num1 * dig_num * (int)pow(10,i));
	i++;
}

printf("%ls\n", result);

return (0);
}
