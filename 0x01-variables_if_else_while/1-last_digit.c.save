#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - print last digit of number
* uses modulo operator and printf
*
* Return: 0 if success
*/

int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	last_digit = n%10	
	return (0);
}
